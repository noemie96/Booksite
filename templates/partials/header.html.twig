

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Le Bouquineur</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor02">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="{{path('homepage') }}">Home
            
          </a>
        </li>
         <ul class='navbar-nav ml-auto'>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-toggle="dropdown" id="booksDropdownLink" href="{{path('books_index') }}">Livres
          </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="booksDropdownLink">
                                <a href="{{ path('books_index', {"filtre":"Thriller"}) }}" class="dropdown-item">Thriller</a>
                                <div class="dropdown-divider"></div>
                                <a href="{{ path('books_index', {"filtre":"Horreur"}) }}" class="dropdown-item">Horreur</a>
                                <div class="dropdown-divider"></div>
                                <a href="{{ path('books_index', {"filtre":"Policier"}) }}" class="dropdown-item">Policier</a>
                                <div class="dropdown-divider"></div>
                                <a href="{{ path('books_index',{"filtre":"Fantasy"}) }}" class="dropdown-item">Fantasy</a>
                                <div class="dropdown-divider"></div>
                                <a href="{{ path('books_index', {"filtre":"Fantastique"}) }}" class="dropdown-item">Fantastique</a>
                                <div class="dropdown-divider"></div>
                                <a href="{{ path('books_index', {"filtre":"Romance"}) }}" class="dropdown-item">Romance</a>
                                <div class="dropdown-divider"></div>
                                <a href="{{ path('books_index', {"filtre":"Sciences-fiction"}) }}" class="dropdown-item">Sciences-fiction</a>
                                <div class="dropdown-divider"></div>
                                <a href="{{ path('books_index',{"filtre":"Classique"}) }}" class="dropdown-item">Classique</a>
                                <div class="dropdown-divider"></div>
                                <a href="{{ path('books_index',{"filtre":"Contemporain"}) }}" class="dropdown-item">Contemporain</a>
                               
                                
        </li>
        
      {% if app.user %}
                        <li class='nav-item dropdown'>
                            <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                                {{app.user.fullName}}
                            </a>
                            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                                <a href="{{ path('books_create')}}" class="dropdown-item">Créer une fiche livre</a>
                                <div class="dropdown-divider"></div>
                                <a href="{{ path('account_index') }}" class="dropdown-item">Mon compte</a>
                                <a href="{{ path('account_profile') }}" class="dropdown-item">Modifer mon profil</a>
                                <div class="dropdown-divider"></div>
                                <a href="{{ path('account_logout') }}" class="dropdown-item">Déconnexion</a>
                            </div>
                        </li>
                    {% else %}
                        <li class='nav-item'>
                            <a href="{{ path('account_register')}}" class="nav-link">Inscription</a>
                        </li>
                        <li class='nav-item'>
                            <a href="{{ path('account_login') }}" class="nav-link">Connexion</a>
                        </li>
                    {% endif %}
      </ul>
      <form class="d-flex" action="{{path('search')}}" method="GET">
        <input class="form-control me-sm-2" type="text" placeholder="Recherche" name="search">
        <button class="btn btn-secondary my-2 my-sm-0" type="submit">Recherche</button>
      </form>
    </div>
  </div>
</nav>



    {# dump(app.flashes) #}

        {% for label, messages in app.flashes%}
            <div class="container">
              <div class="alert alert-{{label}}">
                 {% for message in messages %}
                        <p>{{message | raw }}</p>
                 {% endfor %}
             </div>
            </div>
        {% endfor %}