{% extends 'base.html.twig' %}

{% block title %}Genres{% endblock %}

{% block body %}
    <h1>Genres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ genre.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('genres_index') }}">back to list</a>

    <a href="{{ path('genres_edit', {'id': genre.id}) }}">edit</a>

    {{ include('genres/_delete_form.html.twig') }}
{% endblock %}
