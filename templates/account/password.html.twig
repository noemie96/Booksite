{% extends 'base.html.twig' %}

{% block title %}Modification du profil utilisateur: mot de passe{% endblock %}

{% block body %}
    <div class="container">
    <div class="espace2"></div>
        <h1>Modification du mot de passe</h1>
        {{form_start(myForm)}}
            {{form_widget(myForm)}}
                <button type="submit" class="btn btn-success">Confirmer la modification du mot du passe</button>
        {{form_end(myForm)}}
        <div class="espace2"></div>
    </div>
{% endblock %} 